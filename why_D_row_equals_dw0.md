## 把一行替换为 `D[1,:]`，会不会变成“只取对角项”？它与 \(Dw(\pm 1)=0\) 等价吗？

不会变成只取对角项；并且（在谱配点法的离散意义下）它**就是**在施加 \(w'(\pm 1)=0\)。下面把关键点说清楚。

---

### 1) `D[1,:] * w` 的正确含义：整行点积（用到所有列）

当你在 tau 行替换里写（概念上）：

- 把某一行系数改成 `D[1,:]`

这表示该行方程是

\[
D[1,:] \; w 
= \sum_{j=1}^{n} D[1,j] \, w_j
= 0.
\]

也就是说它使用的是**第一行的所有系数** \(D[1,1],D[1,2],\dots,D[1,n]\)。

你担心的表达式

\[
D[1,1]w_1 + D[2,2]w_2 + \cdots + D[n,n]w_n
\]

其实对应的是

\[
\mathrm{diag}(D)\cdot w
\]

（取对角线形成的向量去做点积），这和 `D[1,:] * w` 是完全不同的运算。

---

### 2) 为什么 `D[1,:] * w = 0` 等价于离散意义下的 \(w'(z_1)=0\)

在 Chebyshev collocation（谱配点法）中，一阶导数矩阵 \(D\) 满足：

\[
\underbrace{w'}_{\text{在各节点处的导数向量}}
\approx
D\,\underbrace{w}_{\text{在各节点处的函数值向量}}.
\]

因此，第一个节点处（边界点）导数就是：

\[
\big(w'\big)_1 \approx (D w)_1 = D[1,:] \, w.
\]

所以把某一行替换成 `D[1,:]` 并令 RHS=0，正是在强制：

\[
(Dw)_1 = 0 \quad\Longleftrightarrow\quad w'(z_1)=0 \;\text{（离散意义）}.
\]

同理，把另一行替换为 `D[n,:]` 并令 RHS=0，就是在强制

\[
(Dw)_n = 0 \quad\Longleftrightarrow\quad w'(z_n)=0.
\]

---

### 3) 小提醒：需要保证 `z` 与 `D` 的重排一致

tau 行替换默认 `z[1]` 与 `z[end]` 是两侧壁面（例如 -1 和 1）。如果你对 `z` 做了反转/重排，也必须对 `D` 做同样重排。

建议在施加 BC 前检查：

```julia
@show z[1] z[end]
```

确认它们确实是边界位置。
