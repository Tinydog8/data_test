## tau（行替换）如何实现边界条件：代码—数学对应

下面解释你代码中的这段 **row replacement / tau** 操作，是如何把边界条件强制施加到离散线性系统里的。

---

### 1. 背景：tau（行替换）是什么

离散后你要解的系统可以写成

\[
A\,x = b,
\]

其中未知量按块堆叠为

\[
x=\begin{bmatrix}u\\ w\end{bmatrix}\in\mathbb{C}^{2n},\qquad
u,w\in\mathbb{C}^{n}.
\]

tau/行替换的核心思想：

- 在边界点处，不再使用 PDE 的离散方程（collocation 方程），而是用 **边界条件方程** 取而代之。
- 数值实现就是：把某一行 `A[row,:]` 清零，然后写入代表边界条件的线性关系，同时把 `b[row]` 改成边界值。

---

### 2. 索引约定：哪些列是 \(u\)，哪些列是 \(w\)

因为

- `x[1:n] = u`
- `x[n+1:2n] = w`

所以：

- 给 \(u\) 加边界条件：只改写 `A[row, 1:n]`（并且让该行不再包含 \(w\) 的列）。
- 给 \(w\) 加边界条件：只改写 `A[row, n+1:2n]`。

---

### 3. \(u(\pm1)=0\) 如何对应到两次行替换

#### 3.1 数学形式

在 Chebyshev collocation 节点上，\(u(z_1)=u_1\)，\(u(z_n)=u_n\)。边界条件

\[
u(z_1)=0,\qquad u(z_n)=0
\]

就是两条线性方程：

\[
[1,0,\dots,0]\,u = 0,\qquad [0,\dots,0,1]\,u=0.
\]

#### 3.2 代码对应

```julia
A[1, :] .= 0
A[1, 1:n] .= I_mat[1, :]
RHS[1, :] .= 0

A[n, :] .= 0
A[n, 1:n] .= I_mat[n, :]
RHS[n, :] .= 0
```

解释：

- `I_mat[1,:] = [1,0,0,...,0]`，因此第 1 行变成 **\(u_1=0\)**。
- `I_mat[n,:] = [0,...,0,1]`，因此第 n 行变成 **\(u_n=0\)**。

这两行不再代表 PDE，而是直接代表 Dirichlet 边界条件。

---

### 4. \(w(\pm1)=0\) 如何对应到两次行替换

#### 4.1 数学形式

\[
w(z_1)=0,\qquad w(z_n)=0.
\]

#### 4.2 代码对应（注意 \(w\) 的自由度在 `n+1:2n`）

```julia
A[n + 1, :] .= 0
A[n + 1, n + 1:2n] .= I_mat[1, :]
RHS[n + 1, :] .= 0

A[2n, :] .= 0
A[2n, n + 1:2n] .= I_mat[n, :]
RHS[2n, :] .= 0
```

解释：

- 第 `n+1` 行只作用在 `w` 的列块，并选出 `w_1`，因此是 **\(w_1=0\)**。
- 第 `2n` 行只作用在 `w` 的列块，并选出 `w_n`，因此是 **\(w_n=0\)**。

---

### 5. \(Dw(\pm1)=0\) 如何由行替换实现（Neumann 条件）

#### 5.1 数学形式

连续边界条件：

\[
w'(z_1)=0,\qquad w'(z_n)=0.
\]

离散时一阶导数矩阵 `D1c` 满足

\[
w' \approx D1c\,w.
\]

所以边界条件等价于

\[
(D1c\,w)_1 = D1c[1,:]\cdot w = 0,\qquad (D1c\,w)_n = D1c[n,:]\cdot w = 0.
\]

#### 5.2 代码对应

```julia
A[n + 2, :] .= 0
A[n + 2, n + 1:2n] .= D1c[1, :]
RHS[n + 2, :] .= 0

A[2n - 1, :] .= 0
A[2n - 1, n + 1:2n] .= D1c[n, :]
RHS[2n - 1, :] .= 0
```

解释：

- 第 `n+2` 行变为 `D1c[1,:] * w = 0`，对应 **\(w'(z_1)=0\)**。
- 第 `2n-1` 行变为 `D1c[n,:] * w = 0`，对应 **\(w'(z_n)=0\)**。

---

### 6. 为什么要先 `A[row,:].=0`，并把 RHS 也置零？

- `A[row,:] .= 0`：确保该行只表达边界条件，不夹带原 PDE 的系数。
- `RHS[row,:] .= 0`：因为这些边界条件都是齐次（边界值为 0）。

如果是非齐次边界（比如 \(u(1)=1\)），那就应把对应 RHS 行设为 1。

---

### 7. 一句话对照表：每次替换对应的边界条件

- `A[1, 1:n] = I[1,:]`  → \(u_1=0\) → \(u(-1)=0\)
- `A[n, 1:n] = I[n,:]`  → \(u_n=0\) → \(u(1)=0\)
- `A[n+1, n+1:2n] = I[1,:]` → \(w_1=0\) → \(w(-1)=0\)
- `A[2n, n+1:2n] = I[n,:]` → \(w_n=0\) → \(w(1)=0\)
- `A[n+2, n+1:2n] = D1[1,:]` → \((D1w)_1=0\) → \(w'(-1)=0\)
- `A[2n-1, n+1:2n] = D1[n,:]` → \((D1w)_n=0\) → \(w'(1)=0\)

---

### 8. 小提醒：务必确认 `z[1]` 与 `z[end]` 真的是边界点

tau 行替换默认 `z[1]`、`z[end]` 是两侧壁面。如果你在构造网格时做了反转/重排，请用

```julia
@show z[1] z[end]
```

确认它们分别对应 -1 和 1（或你的物理边界位置）。
