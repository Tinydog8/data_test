## 为什么 \(Dw=0\) 的边界条件常用“第 2 行”和“第 \(n-1\) 行”来替换？（4×4 例子）

你看到的做法属于 **tau（行替换 / row replacement）**：在离散线性系统里，把部分“PDE 的离散方程行”替换成“边界条件方程行”。

关键点：

- 选择替换 **第 2 行** 和 **第 \(n-1\) 行** 并不是因为导数条件“天然对应这两行”。
- 真正原因是：
  - 第 1 行和第 n 行通常已经用来施加 **Dirichlet 条件** \(w(\pm 1)=0\)；
  - 还需要两条 **Neumann 条件** \(Dw(\pm 1)=0\)；
  - 为了保持系统仍然是方阵（\(n\) 个未知 + \(n\) 条方程），必须再“腾出”两行；
  - 最常用、也比较稳定的选择就是 **靠近边界的两行**：第 2 行与第 \(n-1\) 行。

---

### 1) 4×4 的“仅 w 方程”系统（\(n=4\)）

未知向量

\[
 w=\begin{bmatrix}w_1\\w_2\\w_3\\w_4\end{bmatrix}.
\]

不施加边界条件时，离散系统为

\[
L\,w=r,
\qquad
L\in\mathbb{C}^{4\times 4}.
\]

写成一般矩阵：

\[
\begin{bmatrix}
\ell_{11}&\ell_{12}&\ell_{13}&\ell_{14}\\
\ell_{21}&\ell_{22}&\ell_{23}&\ell_{24}\\
\ell_{31}&\ell_{32}&\ell_{33}&\ell_{34}\\
\ell_{41}&\ell_{42}&\ell_{43}&\ell_{44}
\end{bmatrix}
\begin{bmatrix}w_1\\w_2\\w_3\\w_4\end{bmatrix}
=
\begin{bmatrix}r_1\\r_2\\r_3\\r_4\end{bmatrix}.
\]

这 4 行原本都是“PDE 在 4 个 collocation 点的方程”。

---

### 2) 需要施加的 4 条边界条件（典型 OS 需要 4 条）

对于 \(w\) 的 Orr–Sommerfeld 类型问题，常见边界条件是：

- \(w(-1)=0\Rightarrow w_1=0\)
- \(w(1)=0\Rightarrow w_4=0\)
- \(w'(-1)=0\Rightarrow (Dw)_1=0\)
- \(w'(1)=0\Rightarrow (Dw)_4=0\)

其中 \(D\) 是一阶导数矩阵（4×4），满足 \(w'\approx Dw\)。于是导数条件可写为线性约束：

\[
(Dw)_1 = D[1,:]\,w = 0,\qquad (Dw)_4 = D[4,:]\,w = 0.
\]

---

### 3) tau 行替换：用哪几行来写这 4 条 BC？

你必须把这 4 条边界条件写进一个 4×4 的方程组里，所以需要 **4 行方程**来承载它们。

最常见的分配方式是：

- **第 1 行**：放 \(w_1=0\)（Dirichlet，直接选第一个自由度）
- **第 4 行**：放 \(w_4=0\)
- 剩下两条导数条件 \(Dw=0\) 还需要两行来表达
  - 于是用 **第 2 行** 和 **第 3 行（即 \(n-1\) 行）**来放导数条件

替换后系统变成：

\[
L_{\text{BC}}w=
\begin{bmatrix}
1&0&0&0\\
D[1,1]&D[1,2]&D[1,3]&D[1,4]\\
D[4,1]&D[4,2]&D[4,3]&D[4,4]\\
0&0&0&1
\end{bmatrix}
\begin{bmatrix}w_1\\w_2\\w_3\\w_4\end{bmatrix}
=
\begin{bmatrix}0\\0\\0\\0\end{bmatrix}.
\]

逐行解释：

- 第 1 行：\(w_1=0\)
- 第 2 行：\(D[1,:]w=0\)（对应 \(w'(-1)=0\)）
- 第 3 行：\(D[4,:]w=0\)（对应 \(w'(1)=0\)）
- 第 4 行：\(w_4=0\)

这就回答了你的疑问：

> 之所以是“第 2 行”和“第 \(n-1\) 行”，是因为“第 1 行和第 n 行已用于 \(w=0\)”，为了保持方阵并加入 \(Dw=0\) 的两条约束，只能再替换两行；惯例选靠近边界的第 2 与第 \(n-1\) 行。

---

### 4) 对应到你那份 2n×2n 块系统（避免行号混淆）

在整体未知为 \([u;w]\) 的 2n×2n 系统里，\(w\) 相关的方程行是全局的 \(n+1\) 到 \(2n\) 行。

所以“w 子块的第 2 行”和“w 子块的第 \(n-1\) 行”在全局行号上分别是：

- \(n+2\)（w 子块第 2 行）
- \(2n-1\)（w 子块第 \(n-1\) 行）

这就是你代码里用 `A[n+2, ...]` 和 `A[2n-1, ...]` 放 \(Dw(\pm1)=0\) 的原因。
